#!/bin/python

import os

Cup = "/bin/ip route replace default via {{ vrrp["vip"]["isp1"]["gateway"] }} dev isp1 proto 112 metric 75"
Cdown = "/bin/ip route replace default via {{ vrrp["vip"]["isp2"]["gateway"] }} dev isp2 proto 112 metric 75"

def hook(h, m, Q):
    if m == "121":
        if h == 2:
            print("route_selector:", Cdown)
            os.system(Cdown)
        elif h == 3:
            print("route_selector:", Cup)
            os.system(Cup)
