global_defs {{ "{" }}

{{ "}" }}

vrrp_instance {{ keepalived["instance_name"] }} {{ "{" }}
    state SLAVE
    notify /etc/keepalived/notify.sh
    interface skynet
    virtual_router_id {{ keepalived["id"] }}
    priority 0
    advert_int 1
    authentication {{ "{" }}
        auth_type PASS
        auth_pass {{ keepalived["password"] }}
    {{ "}" }}
    virtual_ipaddress {{ "{" }}
        192.168.128.1
    {{ "}" }}
{{ "}" }}
