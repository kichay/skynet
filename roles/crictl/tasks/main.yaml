---
- name: Unarchive crictl
  unarchive:
    src: "https://github.com/kubernetes-sigs/cri-tools/releases/download/{{ crictl.version }}/crictl-{{ crictl.version }}-linux-amd64.tar.gz"
    dest: /usr/local/bin
    remote_src: yes

- name: Generate crictl config file
  template:
    src: crictl.yaml.j2
    dest: /etc/crictl.yaml
    owner: root
    group: root
    mode: u=rw,g=r,o=r
